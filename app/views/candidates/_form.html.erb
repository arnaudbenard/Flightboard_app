
<%= form_for(@candidate, :url => job_candidates_path(@job)) do |f| %>


  <% if @candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candidate.errors.count, "error") %> prohibited this candidate from being saved:</h2>

      <ul>
      <% @candidate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :resume %><br />
    <%= f.text_area :resume , :rows => 10, :cols => 25 %>
  </div>
  	<div class="field">
	<%= f.label :cv %><br />
	<%= f.file_field :cv %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
